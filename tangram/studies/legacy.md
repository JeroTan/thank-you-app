# Legacy

This is a from-scratch (greenfield) project. There is no existing codebase to integrate with, meaning we have a "Clean Slate" to establish strict standards. The focus is on building an atomic component structure with centralized state management (`src/store/**`) from day one to avoid future regressions.