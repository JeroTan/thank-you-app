# Tangram Codex Skills

The Tangram Build workflow from `.codex/workflows/tangram` has been converted into Codex-ready skill files here.

Codex discovers these skills from `.agents/skills` when this bundle is copied into a project root.

The legacy slash command shape `/tangram:<name>` maps to `$tangram-<name>` in Codex.
